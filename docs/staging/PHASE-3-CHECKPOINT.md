# Phase 3 recovery checkpoint

## Current checkpoint — 2026-09-14

Implementation and real staging verification COMPLETE on clean application commit `d9fdb858dac29e6f8b7f4bc5c4753af3f68d40db`. Final Phase 3 run: 14/14 PASS, 11:14:29–11:15:11 UTC. Exact tombstone cleanup PASS at 11:39:49 UTC; the deliberately late object was removed and a referenced ready photo's hash remained intact. Final original (6 pages), phone (4 pages), and amendment (5 pages) PDFs rendered and every page visually inspected: photos, attribution, text and page breaks readable.

Both additive migrations below are APPLIED; CLI history remains absent. Do not replay them. Existing synthetic data, ignored configuration, `raw/`, and original browser tabs remain preserved. Staging HTTPS launcher session 24254 was left running; verify actual processes/ports again after any interruption. No onboarding or long natural-expiry rerun is needed.

See [final verification](../PHASE-3-VERIFICATION.md) for evidence provenance, actual results and limitations; [implementation/API contract](../PHASE-3-EVIDENCE-EXPORTS.md) for the design handoff and operating rules. The older runner `nextStep` text is superseded by this checkpoint.

Next delivery action: commit these documentation updates, fetch/check divergence, push `astra/production-mvp` without force, and verify CI for the exact pushed SHA. Save final delivery SHA, push/CI result and next task in ignored `.staging/phase3-delivery.json`; consult that file first if delivery is interrupted. After successful delivery the next bounded task is supported account recovery in isolated staging. No production deployment is authorized.

## Preserved recovery journal (historical steps, superseded above)

Starting commit: 0ad8b602e286fabb9114b29e970f083698a7aedd, astra/production-mvp. Only unrelated raw/ is untracked. Existing staging HTTPS server (node24180/11896, launcher3348) and original signed-in Playwright MCP survive; preserve other tabs. Phase2 verification is complete; no onboarding/expiry rerun planned.

Read AGENTS/CLAUDE, installed Next route-handler docs, Phase2 verification, runbook and recovery records. Three existing migration SHA256 values match the recorded journal. Read-only SQL Editor preflight in verified tradesafe-staging yqkiizimbtlygovkscoh confirms13 RLS public tables, expected ts_command,3 templates, unchanged report columns, no Phase3 tables, no Storage buckets/policies and absent CLI history. Original flhsdtshwwuddzyguyhf excluded. No Phase3 SQL applied yet.

Implementation decision: authenticated server proxy for all photo/export bytes; no signed URLs or ordinary Storage privileges. Database metadata retains company/report scope and ordinary read RLS. Trusted byte validation/storage completion uses a server-only staging service credential; it does not replace ordinary-user authorization tests. Pending uploads block finalization; report/company locks serialize reserve/complete/remove/finalize and membership changes. Tombstones permit precise cleanup of removed draft objects; never delete ready/referenced or unknown objects. Photo limits: JPEG/PNG/WebP input,3MiB input,20 megapixels,10 active photos/report, normalized JPEG without EXIF. Client filenames/timestamps are not trusted. Export v1 retains one immutable PDF per snapshot; subsequent action history is explicitly excluded, relationships indexed at export cutoff. Node PDFKit + sharp, no browser runtime or paid renderer in deployment.

Next: implement additive migration and local transaction tests; retrieve existing staging server credential securely if available, without printing it; validate/review migration before one SQL Editor execution. Keep this file current after each major operation.

2026-09-14: local image decoding/metadata/size tests and PGlite evidence/export transaction tests PASS. Server routes and functional photo/export UI implemented. Existing staging service-role key copied privately from the exact project's Dashboard into new ignored .staging/server.env, with JWT role/reference checked and clipboard cleared; existing .env/.env.staging.local untouched. This key is server-only; ordinary test clients never inherit it. Re-read installed Next external-package/output-tracing docs; PDFKit/sharp externalized and font asset traced.

Reviewed migration 20260913000100_private_evidence_exports.sql SHA2566153aaf9f97e1ee1e4424bc6821e37e2bb61265274c72122676f5ea149621dcb. Local transaction tests cover RLS, direct completion/Storage denial, pending-finalization barrier, final immutability, removed pending completion, separate amendment evidence, export lease/attempt guards and retained snapshot retry. Read-only pre-application check confirms Phase3 objects absent, original snapshot trigger present, no ordinary workflow writes, no buckets and no CLI history. Next authorized operation: apply this one reviewed migration through verified staging SQL Editor, then inspect actual objects/grants. Do not replay prior migrations.

2026-09-14: Phase3 migration 20260913000100_private_evidence_exports.sql (SHA2566153aaf9f97e1ee1e4424bc6821e37e2bb61265274c72122676f5ea149621dcb) submitted ONCE in exact staging SQL Editor; returned Success, no rows. Do not reapply. Next: inspect catalog/RLS/grants/buckets, restart only staging app to load private server credential, and run real ordinary-session Phase3 scenarios.

Migration postcheck PASS: both new tables have RLS, zero ordinary direct-write grants; ordinary completion/export RPC denied, service completion allowed; both buckets private with3MiB/32MiB limits; restrictive Storage policy covers anon/authenticated. No photo/export rows existed before tests. Staging HTTPS restarted with new server-only configuration, exec24254. Next: real ordinary-session photo/export tests serially, including interrupted pending reconciliation and a deliberately missing-object synthetic export fixture.

First real Phase3 run PASS12/12 (2026-09-14 05:35:57-05:36:34 UTC), base0ad8b60/dirty. All test IDs/checks persisted in .staging/phase3-verification.json and timestamped staging-runs JSON; earlier artifacts archived under test-results/phase3/[run timestamp]. Actual6-page original and4-page phone PDFs rendered with retained Poppler and every page visually inspected, including photos/attribution/page breaks. Extending final checks for actual simultaneous upload/finalize, exact Storage errors and finalized amendment export before final source verification. No Phase2 onboarding/long expiry repeated.

Safe cleanup inventory stopped without deletion: actual staging42501 on service-role report SELECT. Existing permissions correctly omit broad privileged Data API report reads. Forward repair20260914000100_evidence_cleanup_guard.sql SHA2567e51eef2d40a27dabdb5d244cf2a0b7cc87396a23fd43b2d2911a1e571c0a22f adds a narrow service-only candidate function, verifies tombstone/snapshot references under locks and returns only exact object path/id. App/operator cleanup now use it. Local tests PASS, including ordinary execute denial and ready-evidence rejection. Next: verify function absent, apply this single additive repair, recheck grants, then final Phase3 run and cleanup verification. Original Phase3 migration remains unchanged.

Cleanup forward repair applied once; SQL Editor returned Success. Repaired live inventory PASS:2 removed rows safely checked, no deletion; broad service-role report SELECT remains absent. All14 expanded Phase3 checks previously passed, plus Phase2API15/15, serial browser8/8,30local tests,23smoke checks, lint/typecheck/build. PDFKit/sharp/font confirmed in production route trace. Audit zero high/critical,1 existing moderate unused Anthropic tool advisory. Next: commit reviewed application, execute final expanded Phase3 checks on that commit, exercise exact tombstone cleanup, inspect final PDFs including amendment, record verification and push/CI.
