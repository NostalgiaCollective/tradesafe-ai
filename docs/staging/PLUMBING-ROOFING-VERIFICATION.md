# Plumbing and roofing journey verification

The existing application creates reports from the selected published template and reads the saved template snapshot when resuming, grouping observations, counting answers, linking review errors and rendering PDFs. Plumbing has 15 observations; roofing has 18. Corrections copy the original template relationship and document into a separate amendment. No electrical-specific product behavior was found during code inspection; the concrete gap was executed cross-trade browser coverage.

`tests/local-browser/trades.spec.mjs` adds one complete WebKit journey for each trade to the existing disposable Ubuntu/local-Supabase job. It uses provider-assisted local synthetic owner, worker, supervisor and outsider accounts, local-only network guards and ordinary-user application/RPC writes. No hosted account, environment, mailbox or project is involved.

Each journey selects the trade in the actual interface, saves and resumes from the dashboard, checks untouched unanswered defaults, exact template/questions/section counts, all answer states, explanation requirements (including independent server rejection), optional controls, filtered/collapsed review focus, Back/Continue and saved reload. It uploads a recognizable captioned image through the browser, verifies retained normalization and reload, finalizes, opens/downloads the PDF and extracts all trade questions, explanations and caption. It saves a separate correction with its own photo, checks template/parent relationships, updates the originating action, exercises supervisor verification, checks audit events and rejects worker closure. It compares the entire finalized original and retained photo/PDF hashes, then checks cross-company and revoked-access denial.

The required source browser suite now executes six tests; zero skipped tests are permitted. The existing required recovery regression is preserved, not a new recovery milestone. Screenshot capture is supplemental only; executed assertions and sanitized trade receipts are the evidence. No private test artifact or credentials are uploaded. A failing browser workflow remains a failed gate.

No application changes, migrations, template text, version IDs, regulatory references or qualified-review warnings are changed by this coverage. If execution demonstrates an application defect, its fix and hosted verification must be recorded separately. Tests do not establish technical correctness or compliance.

Existing content-review concerns remain for qualified reviewers: legacy credential terminology (including “College of Trades”), numeric/code-related plumbing statements and broad roofing compliance/safety statements. This milestone does not adjudicate their correctness or publish replacement requirements. Preserve the current version and historical snapshots until a qualified new-version review is authorized.

Exact commit/CI outcomes and any concrete findings belong in `.staging/reliability-workflow-checkpoint.json` and `.staging/plumbing-roofing-delivery.md`. Test-only changes require no staging deployment. Local WebKit results are not hosted Chromium checks, actual email delivery or physical-phone acceptance. Daniel's praise does not close the preceding daily follow-up phone check; that remains pending.
