import {readFileSync,writeFileSync,mkdirSync} from 'node:fs';
import {resolve} from 'node:path';
import assert from 'node:assert/strict';
process.env.PLAYWRIGHT_BROWSERS_PATH=resolve('.staging/browsers');
const {webkit,chromium,devices,expect}=await import('@playwright/test');
const a=JSON.parse(readFileSync('.staging/recovery-email-account.json')),g=JSON.parse(readFileSync('.staging/hosted-access.json'));
const hosted=process.argv.includes('--hosted'),origin=hosted?'https://tradesafe-staging-yqkiizimbtlygovkscoh.onrender.com':'https://localhost:3000';
const result={kind:'Chromium/WebKit mobile emulation; synthetic intercepted uploads; no draft writes',origin,checks:[]};
for(const [engineName,engine] of [['webkit',webkit],['chromium',chromium]]){
const b=await engine.launch(),c=await b.newContext({...devices['iPhone 14 Pro Max'],...(hosted?{httpCredentials:{username:g.username,password:g.password}}:{})}),p=await c.newPage();
try{
await p.goto(origin+'/auth/login',{waitUntil:'networkidle'});await p.getByLabel('Email',{exact:true}).fill(a.email);await p.getByLabel('Password',{exact:true}).fill(a.currentPassword);await p.getByRole('button',{name:'Sign In',exact:true}).click();await p.waitForURL('**/dashboard');
const fixture=JSON.parse(readFileSync('.staging/photo-real-verification.json'));
await p.goto(origin+'/report/'+fixture.reportId+'?step=3',{waitUntil:'networkidle'});
const observations=await p.getByLabel('Observation',{exact:true}).evaluateAll(es=>es.map(e=>e.value));assert.ok(observations.length>0&&observations.every(v=>v==='unanswered'));result.checks.push(engineName+': actual draft UI with unanswered observations');
if(hosted){await expect(p.getByTestId('upload-diagnostics')).toContainText('Client: ready / photo-trace-1');await expect(p.getByTestId('upload-diagnostics')).toContainText('Server now: '+process.env.EXPECTED_COMMIT.slice(0,7));await expect(p.getByTestId('upload-diagnostics')).toContainText('Page build: '+process.env.EXPECTED_COMMIT.slice(0,7));}
assert.equal(await p.locator('#evidence-file').count(),1);
const base=origin+'/api/reports/'+new URL(p.url()).pathname.split('/')[2]+'/evidence';
let fault='validation',posts=0,saved=false,ids=[];
await c.route(base,async r=>{if(r.request().method()==='POST'){posts++;ids.push(r.request().headers()['x-evidence-id']);if(fault==='network')return r.abort();if(fault==='success'){await new Promise(resolve=>setTimeout(resolve,600));saved=true;return r.fulfill({status:200,contentType:'application/json',body:'{"state":"ready"}'})}return r.fulfill({status:fault==='session'||fault==='gate'?401:fault==='denied'?403:422,headers:fault==='gate'?{'www-authenticate':'Basic realm="staging"'}:{},contentType:'application/json',body:JSON.stringify({error:fault==='denied'?'You do not have permission for this action.':'Synthetic image validation rejection'})})}return r.fulfill({status:saved?503:200,contentType:'application/json',body:saved?'{}':'[]'})});
const emptyUpload=p.getByRole('button',{name:'Upload photo',exact:true});await emptyUpload.tap();await expect(p.getByRole('alert').filter({hasText:'Choose the original'})).toBeInViewport();assert.equal(posts,0);result.checks.push(engineName+': empty selection explains why instead of silently disabling');
await p.locator('#evidence-file').setInputFiles({name:'synthetic.jpeg',mimeType:'image/jpeg',buffer:Buffer.alloc(3*1024*1024+1)});await p.locator('#evidence-caption').fill('Synthetic fault test');
const upload=p.getByRole('button',{name:'Upload photo',exact:true});await upload.scrollIntoViewIfNeeded();assert.equal(await upload.evaluate(e=>{const r=e.getBoundingClientRect();return e.contains(document.elementFromPoint(r.x+r.width/2,r.y+r.height/2))}),true);result.checks.push(engineName+': button hit-test unobstructed');assert.equal(await upload.evaluate(e=>!!e.form),false);await tap();await expect(p.getByRole('alert').filter({hasText:'3 MiB'})).toBeInViewport();assert.equal(posts,0);result.checks.push(engineName+': size rejection visible, no request');
await p.locator('#evidence-file').setInputFiles({name:'synthetic.jpeg',mimeType:'image/jpeg',buffer:Buffer.from('synthetic')});
async function tap(){try{await upload.tap()}catch{console.log(JSON.stringify({engineName,fault,posts,checks:result.checks,state:await upload.evaluate(e=>{const r=e.getBoundingClientRect();return {disabled:e.matches(':disabled'),rect:{x:r.x,y:r.y,width:r.width,height:r.height},scrollY,viewport:innerHeight,hit:e.contains(document.elementFromPoint(r.x+r.width/2,r.y+r.height/2)),scrollBehavior:getComputedStyle(document.documentElement).scrollBehavior}})}));throw Error('Touch interaction failed; sanitized diagnostic above')}}
for(fault of ['validation','session','gate','denied','network']){
await tap();const text={validation:'Synthetic image',session:'session has expired',gate:'Staging access',denied:'permission',network:'Check your connection'}[fault];await expect(p.getByRole('alert').filter({hasText:text})).toBeInViewport();assert.equal(await p.locator('#evidence-file').evaluate(e=>e.files.length),1);assert.equal(await p.locator('#evidence-caption').inputValue(),'Synthetic fault test');result.checks.push(engineName+': '+fault+' visible; file/caption retained');
}
fault='success';await tap();await expect(p.getByRole('status').filter({hasText:'Uploading photo'})).toBeVisible();await expect(p.getByRole('status').filter({hasText:'Photo saved and retained.'})).toBeInViewport();await expect(p.getByRole('alert').filter({hasText:'list could not refresh'})).toBeInViewport();assert.equal(await p.locator('#evidence-file').evaluate(e=>e.files.length),0);assert.equal(new Set(ids).size,1);result.checks.push(engineName+': progress, acknowledged success survives refresh failure, same retry ID');
if(hosted){await expect(p.getByTestId('upload-diagnostics')).toContainText('pointer received');await expect(p.getByTestId('upload-diagnostics')).toContainText('click handler');await expect(p.getByTestId('upload-diagnostics')).toContainText('request started');await expect(p.getByTestId('upload-diagnostics')).toContainText('response HTTP 200');await expect(p.getByTestId('upload-diagnostics')).toContainText('save acknowledged');result.checks.push(engineName+': staging trace reaches rendered success');}
await c.unroute(base);
const noJs=await b.newContext({...devices['iPhone 14 Pro Max'],javaScriptEnabled:false,storageState:await c.storageState(),...(hosted?{httpCredentials:{username:g.username,password:g.password}}:{})});const staticPage=await noJs.newPage();await staticPage.goto(origin+'/report/'+fixture.reportId+'?step=3');await expect(staticPage.getByLabel('Photo file',{exact:true})).toBeDisabled();await expect(staticPage.getByRole('button',{name:'Preparing photo controls...'})).toBeDisabled();if(hosted){await expect(staticPage.getByTestId('upload-diagnostics')).toContainText('Page build: '+process.env.EXPECTED_COMMIT.slice(0,7));await expect(staticPage.getByTestId('upload-diagnostics')).toContainText('Client: waiting');}await noJs.close();result.checks.push(engineName+': server-rendered controls locked until hydration, loading explanation visible');
await c.clearCookies();assert.equal((await p.request.get(base)).status(),401);result.checks.push(engineName+': real missing-session evidence authorization denied');
}finally{await b.close()}}
result.status='PASS';mkdirSync('test-results/photo-feedback',{recursive:true});writeFileSync('test-results/photo-feedback/'+(hosted?'hosted':'local')+'.json',JSON.stringify(result,null,2));console.log(JSON.stringify(result));
