import {applicablePrompts,contentStateLabel,safeSourceUrl} from './brief-content.mjs'
const e=value=>String(value??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))
export function contentExport(snapshot){
 const c=snapshot.content
 if(!c)return `<h2>Content provenance</h2><p>Recorded source version: ${e(snapshot.sourceVersion||'Unknown')}. Detailed prompt and review provenance unknown; no later metadata has been attached.</p>`
 const p=c.payload,prompts=applicablePrompts(c,snapshot.document),decision=c.decision
 return `<h2>Content provenance at recording</h2><p>${e(c.version)} — ${e(contentStateLabel(c))}</p><p>Authored by ${e(c.created_by)}; registered ${e(c.created_at)}.</p><p>${e(p.applicability)}</p><p>${e(p.limits)}</p>${decision?`<p>Decision by ${e(decision.reviewer_name)} at ${e(decision.recorded_at)}. Scope: ${e(decision.qualification_scope)}. Reference: ${e(decision.decision_ref)}.</p>`:'<p>No qualified review decision recorded.</p>'}${prompts.length?prompts.map(x=>`<section><h3>${e(x.title)} — ${e(x.id)} v${e(x.version)}</h3><p>${e(x.wording)}</p><p>Applicability: ${e(x.applicability)}</p><p>Rationale: ${e(x.rationale)}</p><p>Limits: ${e(x.limits)}</p>${x.sourceIds.map(id=>{const s=p.sources.find(s=>s.id===id),url=safeSourceUrl(s.url);return `<p>${e(s.kind)} — ${e(s.authority)}: ${url?`<a href="${e(url)}">${e(s.title)}</a>`:e(s.title)}; ${e(s.section)}. ${e(s.jurisdiction)}. ${s.consolidationFrom?`Consolidation from ${e(s.consolidationFrom)}; currency through ${e(s.currencyThrough)}. `:''}${s.revised?`Revised ${e(s.revised)}. `:''}Retrieved ${e(s.retrieved)}. ${e(s.verification)}</p>`}).join('')}</section>`).join(''):'<p>No task pilot was selected for this assessment. Hazard/control entries are user-entered records.</p>'}`
}
