import {contentExport} from './content-export.mjs'
const escape=value=>String(value??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))
export function briefExport({version,members,acknowledgements,reviews,actions},asOf){
 const d=version.snapshot.document,e=escape,name=id=>version.snapshot.people?.find(p=>p.id===id)?.name||members.find(m=>m.user_id===id)?.display_name||'Former member'
 const row=(label,value)=>`<p><strong>${e(label)}:</strong> ${e(value)}</p>`
 return `<!doctype html><html lang="en"><meta charset="utf-8"><meta name="viewport" content="width=device-width"><title>Daily site brief — version ${version.version}</title><style>body{font:16px/1.5 system-ui;max-width:760px;margin:2rem auto;padding:0 1rem;color:#111}section{break-inside:avoid;border-top:1px solid #aaa;padding:1rem 0}p{white-space:pre-wrap;overflow-wrap:anywhere}@media print{body{margin:0}}</style><body><h1>Daily site brief</h1><p>DRAFT SAFETY CONTENT — pending qualified review. This record does not certify compliance or authorize work. Installation reports do not constitute a complete site safety assessment.</p>${row('Site',d.site)}${version.snapshot.site?row('Site name captured',version.snapshot.site.name)+row('Site location captured',version.snapshot.site.address)+row('Site instructions captured (company supplied)',version.snapshot.site.instructions)+row('Site detail revision',version.snapshot.site.revision)+row('Site association',version.snapshot.siteAssociation?.kind+' at '+version.snapshot.siteAssociation?.linkedAt):''}${row('Work date',d.date)}${row('Company',version.snapshot.company)}${row('Brief ID',version.brief_id)}${row('Assessment version',version.version)}${row('Prompt source version',version.snapshot.sourceVersion)}${contentExport(version.snapshot)}${row('Recorded by',name(version.recorded_by))}${row('Server recorded at',version.recorded_at)}${row('User-confirmed context',d.jurisdiction+' / '+d.workplace)}<p>App roles are not statutory appointments.</p><h2>Today’s work</h2>${row('Task',d.task)}${row('Responsible site contact',d.contact)}${row('Participating crew',d.crew.map(name).join(', '))}<h2>Hazards and controls</h2>${d.steps.map((s,i)=>{const review=reviews.find(r=>r.item_id===s.id);return `<section><h3>${i+1}. ${e(s.task)}</h3>${row('Entry ID',s.id)}${row('Hazard',s.hazard)}${row('Control',s.control)}${row('Control status',s.controlState==='proposed'?'Proposed — not confirmed implemented':'Reported implemented')}${row('Control responsibility recorded',name(s.responsible))}${row('Unresolved concern',s.unresolved?'Yes':'Not recorded')}${row('Supervisor review of this version',review?name(review.reviewed_by)+' at '+review.reviewed_at:'None recorded')}</section>`}).join('')}${row('Work pause',d.paused?d.pauseReason:'Not recorded')}${row('Communication reported by recorder',d.communication||'None recorded; saving did not notify anyone')}<p>A pause record is not the statutory work-refusal process.</p><h2>Crew briefing</h2>${row('Discussion',version.briefing_note)}${row('Attendance entered by '+name(version.recorded_by),version.attendance.map(name).join(', ')||'None recorded')}<h3>Authenticated acknowledgement of version ${version.version}</h3>${acknowledgements.map(a=>row(name(a.user_id),a.acknowledged_at+' (server timestamp)')).join('')||'<p>None recorded.</p>'}<p>Attendance and authenticated acknowledgement are distinct. Neither establishes understanding, qualification or a digital signature. Acknowledgements do not transfer to revised assessments.</p><h2>Follow-up status at export</h2>${row('Export as of',asOf)}<p>These current action states may be later than the assessment. The original finding remains linked to its first recorded version. No notification is implied.</p>${actions.map(a=>row('Action '+a.id,`${a.observation} | ${a.state} | responsible: ${name(a.responsible_id)} | originating version: ${a.brief_version} | progress: ${a.resolution||'None recorded'}`)).join('')||'<p>No linked actions.</p>'}</body></html>`
}
