import RecoveryForm from '../RecoveryForm'
export const metadata={title:'Reset password | TradeSafe',robots:{index:false,follow:false},referrer:'no-referrer'}
export default function ForgotPasswordPage(){return <RecoveryForm requestOnly/>}
