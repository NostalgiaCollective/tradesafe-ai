'use client'
import {participation} from '@/lib/domain/crew.mjs'
import {useRef} from 'react'
import {useOperation} from '@/lib/client/useOperation'
import {briefCommand} from '@/lib/client/brief-command'
import ContentSources from '../ContentSources'
import {applicablePrompts} from '@/lib/domain/brief-content.mjs'
import {ACTION_LABELS} from '@/lib/domain/action-edit'
export default function BriefRecord({brief,version,versions,members,acknowledgements,reviews,actions,actor,role,editable,returnTo,siteId}){
 const op=useOperation(),pending=useRef(null),currentHref='/briefs/'+brief.id+(returnTo?'?'+new URLSearchParams({from:returnTo}):'')
 if(!version)return <><h1>Recorded version not found</h1><a href={currentHref}>Open current brief</a></>
 const d=version.snapshot.document,current=brief.lifecycle==='recorded'&&brief.revision===version.version
 const name=id=>version.snapshot.people?.find(p=>p.id===id)?.name||members.find(m=>m.user_id===id)?.display_name||'Former member'
 const responses=participation(version,members,acknowledgements),latest=versions[0],missing=responses.filter(r=>!r.acknowledgedAt)
 const ack=acknowledgements.some(a=>a.user_id===actor)
 const actionHref=focus=>'/actions?'+new URLSearchParams({company:brief.company_id,mine:'0',...(siteId?{site:siteId}:{}),...(focus?{status:'all',focus}:{}),...(returnTo?.startsWith('/my-work?')?{from:returnTo}:{})})
 function run(command,itemId){void op.run('Saving…',async()=>{
  pending.current||={command,payload:{companyId:brief.company_id,id:brief.id,revision:brief.revision,version:version.version,itemId,requestId:crypto.randomUUID()}}
  await briefCommand(pending.current.command,pending.current.payload,actor)
  // Load authoritative version/acknowledgement state with a fresh client component.
  // eslint-disable-next-line @next/next/no-location-assign-relative-destination
  window.location.assign('/briefs/'+brief.id+'?'+new URLSearchParams({...((command==='revise')?{}:{version:String(version.version)}),...(returnTo?{from:returnTo}:{})}))
 })}
 const locked=!op.ready||op.busy||Boolean(op.error)
 return <article className="brief-journey"><p className="eyebrow">Recorded daily brief · version {version.version}</p><h1>{d.site}</h1><p>{d.date} · {version.snapshot.company}</p><p>Recorded by {name(version.recorded_by)} at {new Date(version.recorded_at).toISOString()}. Content: {version.snapshot.sourceVersion||'Unknown'} (recorded provenance; no work authorization).</p>
 {!current&&<p className="work-notice">This is a previous version. Its acknowledgements do not apply to the current revision. <a href={currentHref}>Open current brief</a></p>}
 <div className="work-buttons"><a className="button" href={'/api/briefs/'+brief.id+'/export?version='+version.version}>Export daily brief</a>{editable&&current&&<button disabled={locked} onClick={()=>run('revise')}>Revise brief</button>}</div><p>Revisions preserve this original. A revised briefing needs fresh acknowledgement; attendance and briefing notes must be recorded again.</p>
 <h2>Today’s work</h2><p>{d.task}</p><p>Responsible site contact: {d.contact}</p><p>User-confirmed context: {d.jurisdiction} · {d.workplace}. App roles do not establish legal roles.</p><p>Participating crew: {d.crew.map(name).join(', ')}</p>
 <details><summary>Recorded prompt content and sources</summary>{!version.snapshot.content&&<p>Historical source version: {version.snapshot.sourceVersion||'Unknown'}. Detailed prompt/review provenance was not captured; no later metadata has been attached.</p>}<ContentSources content={version.snapshot.content} prompts={applicablePrompts(version.snapshot.content,d)}/></details><h2>Hazards and controls</h2>{d.steps.map((s,i)=>{const review=reviews.find(r=>r.item_id===s.id),action=actions.find(a=>a.item_id===s.id);return <section className="work-panel" key={s.id} id={'hazard-'+s.id}><h3>{i+1}. {s.task}</h3><p><strong>Hazard:</strong> {s.hazard}</p><p><strong>Control:</strong> {s.control}</p><p>Control responsibility recorded: {name(s.responsible)}</p><p>{s.controlState==='proposed'?'Proposed — not confirmed implemented':'Reported implemented'}</p>{review?<p>Review recorded by {name(review.reviewed_by)} at {new Date(review.reviewed_at).toISOString()} for version {review.version}.</p>:<p>No supervisor review recorded for this control in this version.</p>}{current&&['owner','supervisor'].includes(role)&&s.controlState==='reported_implemented'&&!review&&<button disabled={locked} onClick={()=>run('review_control',s.id)}>Record supervisor review of control {i+1}</button>}<p>{s.unresolved?'Unresolved concern recorded':'No unresolved concern recorded for this entry'}.</p>{action&&<p><a href={actionHref(action.id)}>Open follow-up action</a> · Current status: {ACTION_LABELS[action.state]} · Responsible: {name(action.responsible_id)}</p>}</section>})}
 <p>Control reviews are recorded observations, not approval to work or a compliance determination.</p><p>{d.paused?'Work recorded as paused: '+d.pauseReason:'No work pause recorded.'}</p><p>Reported communication: {d.communication||'None recorded. Saving did not notify anyone.'}</p><p>A work pause entry is separate from the statutory work-refusal process.</p>
 <h2 id="crew-responses" tabIndex={-1}>Crew briefing</h2><p>{version.briefing_note}</p><p>Attendance entered by {name(version.recorded_by)}: {version.attendance.map(name).join(', ')||'None recorded'}</p><h3>Included participants and responses</h3><p>{responses.length} included participants; {responses.length-missing.length} acknowledged; {missing.length} without acknowledgement for version {version.version}.</p><ul>{responses.map(r=><li key={r.id}>{r.name}: {r.acknowledgedAt?'Acknowledged at '+new Date(r.acknowledgedAt).toISOString():'Not acknowledged'}{!r.active?' (access removed; historical participant)':''}</li>)}</ul>{latest&&latest.version>version.version&&<p className="work-notice">Newer recorded version {latest.version} requires its own acknowledgement. <a href={'/briefs/'+brief.id+'?'+new URLSearchParams({version:String(latest.version),...(returnTo?{from:returnTo}:{})})+'#crew-responses'}>Read newest recorded version</a></p>}<h3>Authenticated acknowledgements — version {version.version}</h3>{acknowledgements.length?<ul>{acknowledgements.map(a=><li key={a.user_id}>{name(a.user_id)} · {new Date(a.acknowledged_at).toISOString()}</li>)}</ul>:<p>No worker acknowledgements recorded for this version.</p>}<p>Attendance entered by another person differs from your own authenticated acknowledgement. Neither proves understanding, qualification or a digital signature.</p>{current&&d.crew.includes(actor)&&(ack?<p role="status">Your acknowledgement is recorded for version {version.version}.</p>:<button className="primary" disabled={locked} onClick={()=>run('acknowledge')}>Acknowledge this version</button>)}
 <div aria-live="polite">{op.message&&<p role="status">{op.message}</p>}{op.error&&<div role="alert"><p>{op.error}</p><p>No new result has been confirmed. Retry the same operation or open the current brief to compare; a revised brief must be read again.</p><button disabled={op.busy} onClick={()=>run(pending.current.command,pending.current.payload.itemId)}>Retry same operation</button><a href={currentHref}>Open current brief</a></div>}</div>
 <h2>Outstanding actions</h2><p>Actions retain the finding from the version that first created them. Later revisions do not silently overwrite or reopen them. Review the linked action when circumstances change. Nothing here sends a notification.</p><a className="button" href={actionHref()}>View outstanding actions</a>
 <details><summary>Recorded version history ({versions.length}{versions.length===50?' most recent':''})</summary><ul>{versions.map(v=><li key={v.version}><a href={'/briefs/'+brief.id+'?'+new URLSearchParams({version:String(v.version),...(returnTo?{from:returnTo}:{})})}>Version {v.version}</a> · {new Date(v.recorded_at).toISOString()}</li>)}</ul></details></article>
}
